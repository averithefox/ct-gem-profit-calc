{
  "dependencies": {
    "arkregex": "^0.0.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "bun build src/index.ts --target browser --outfile dist/index.js && cat metadata.json | jq \".entry=\\\"index.js\\\" | .version=\\\"$(./version.sh)\\\"\" > dist/metadata.json"
  }
}
